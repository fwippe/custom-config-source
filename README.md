# Custom `ConfigSource` demo

This project demonstrates a failing integration test for custom `ConfigSource` implementations with Quarkus 2.2.2.final.
Use following script to reproduce:

```shell
$ ./mvnw verify
```
